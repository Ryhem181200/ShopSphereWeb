<form method="post" action="{{ path('app_stock_delete', {'stockid': stock.stockid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.stockid) }}">
    <button class="btn">Delete</button>
</form>
