{% extends 'base.html.twig' %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h1 class="mb-0">Edit Commande</h1>
                    </div>
                    <div class="card-body">
                        {{ include('commande/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_commande_index') }}" class="btn btn-secondary">Back to List</a>
                    </div>
                </div>
                
                <div class="card mt-4">
                    <div class="card-header">
                        <h3 class="mb-0">Delete Commande</h3>
                    </div>
                    <div class="card-body">
                        {{ include('commande/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
