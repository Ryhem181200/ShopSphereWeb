<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture de vente</title>
    <style>
        /* Styles CSS pour le PDF */
        body {
            font-family: Arial, sans-serif;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px; /* Ajoute une marge en bas du tableau */
        }
        th, td {
            border: 1px solid #dddddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        /* Style pour l'en-tête */
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        /* Style pour chaque ligne du tableau */
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        /* Style pour la première colonne du tableau */
        td:first-child {
            font-weight: bold;
        }
        /* Style pour la dernière colonne du tableau */
        td:last-child {
            text-align: right;
        }
    </style>
</head>
<body>
    <h1>Facture de vente</h1>
    <table>
        <tr>
            <th>Produit</th>
            <th>Quantité vendue</th>
            <th>Montant total</th>
            <th>Date</th>
        </tr>
        <tr>
            <td>{{ vente.idProduit.nom }}</td>
            <td>{{ vente.quantiteVendu }}</td>
            <td>{{ vente.montanttotal }}</td>
            <td>{{ vente.datevente|date('Y-m-d') }}</td>
        </tr>
    </table>
</body>
</html>
